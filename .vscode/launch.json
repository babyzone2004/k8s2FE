{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "game",
            "request": "launch",
            "command": "pnpm run start",
            "cwd": "${workspaceFolder}/apps/Server/game"
        }
    ]
}